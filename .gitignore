# large artifacts stay on the training server (ARC): checkpoints & tokenized data
*.pt
*.npy
__pycache__/
nohup.out
*.pyc

# EXCEPTION: 9B retrofit ckpts are tiny (14MB each, only R+decay trained) -> back up in-repo
!scale/nemo9b_rot_*.pt
